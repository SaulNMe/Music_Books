import getSum from 'music-book/utils/get-sum';
import { module, test } from 'qunit';

module('Unit | Utility | getSum', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = getSum();
    assert.ok(result);
  });
});
