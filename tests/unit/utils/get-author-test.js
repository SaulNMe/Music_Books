import getAuthor from 'music-book/utils/get-author';
import { module, test } from 'qunit';

module('Unit | Utility | getAuthor', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = getAuthor();
    assert.ok(result);
  });
});
